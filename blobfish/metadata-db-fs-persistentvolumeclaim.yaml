apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: metadata-db-fs
  name: metadata-db-fs
spec:
  storageClassName: manual
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
