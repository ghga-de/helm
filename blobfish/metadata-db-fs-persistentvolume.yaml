apiVersion: v1
kind: PersistentVolume
metadata:
  name: metadata-db-fs
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 100Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/var/lib/docker/volumes/metadata_db_fs"
